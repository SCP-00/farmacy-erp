# ══════════════════════════════════════════════════════════
#  FARMACY — Secret Scanning (Gitleaks)
#
#  Escanea secrets en cada PR y push a main.
#  Usa la configuración personalizada en .gitleaks.toml (raíz del repo:
#  gitleaks-action solo autodetecta la raíz y `config-path` debe apuntar ahí).
# ══════════════════════════════════════════════════════════

name: Secret Scanning

on:
  push:
    branches: [main]
  pull_request:  # Todos los PRs, sin importar rama destino

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gitleaks:
    name: Gitleaks scan
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v3
        with:
          config-path: .gitleaks.toml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_ENABLE_COMMENTS: "true"
          GITLEAKS_NOTIFY_USER_LIST: ${{ github.actor }}

      - name: Check Gitleaks exit code
        if: failure()
        run: |
          echo "❌ Secret scanning detectó posibles credenciales en el código."
          echo "   Revisa los resultados en la pestaña Security del repositorio."
          echo "   Si es un falso positivo, agrega la ruta a .gitleaks.toml [allowlist]."
          exit 1
